DATABASE_URL=sqlite:///./data/bet_admin.db
JWT_SECRET=change-me-to-a-long-random-string
JWT_ALG=HS256
JWT_TTL_MIN=480
APP_ENV=development
CORS_ORIGINS=http://localhost:3000
LOGIN_MAX_FAILURES=5
LOGIN_LOCK_MINUTES=15
# Only used by `python -m app.seed` / bootstrap when creating the first admin (min 12 chars).
ADMIN_INITIAL_PASSWORD=
